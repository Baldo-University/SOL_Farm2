collector: avviene l'errore di ___ pfds[i].revents != POLLIN __ che non dovrebbe avvenire
collector: forse non stampa sempre tutti i file
problemi di tempismo?

prova: 	invece di mandare un messaggio di chiusura, il server riceve un evento di scrittura pari a zero.
		in questo modo non si alloca nemmeno memoria, si chiude la socket e non c'e' bisogno di DISCONNECT

nota: se gli errori si rompono prova a mettere SIG_BLOCK invece di SIG_SETMASK nella maschera del master
